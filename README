Please run test with testng.xml from your IDE, or from command line, assuming you have TestNG in your classpath and latest version of FireFox installed, with following command:
java org.testng.TestNG testng.xml

In test classes are used Selenium and TestNG libraries